---
dontChroot: false
timeout: 15
verbose: false

script:
  - command: |
      if ! grep -q '^\[churros\]$' /etc/pacman.conf; then
        printf '\n[churros]\nSigLevel = Optional TrustAll\nServer = file:///root/packages\n' >> /etc/pacman.conf
      fi
    timeout: 10
